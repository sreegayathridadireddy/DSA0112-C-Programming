//medium sorted array

#include<iostream>
using namespace std;
class number{
	public:
		int m,n;
		number(int start,int end):m(start),n(end){}
};
class skipper:public number{
public:
	int k;
	skipper(int start,int end,int skip):number(start,end),k(skip){}
	void print(){
		for(int i=m;i<=n;i+=(k+1)){
			cout<<i<<" ";
		}
		cout<<endl;
	}
};
int main(){
	int m=40,n=100,k=5;
	skipper obj(m,n,k);
	obj.print();
	return 0;
}



//permutations using string


#include<iostream>
#include<exception>
using namespace std;
int main(){
	try{
		int x,y;
		cout<<"Enter the values of x and y: ";
		cin>>x>>y;
		
		if(y==0){
			throw runtime_error("Division by zero error!");
		}
		cout<<"Result of division is "<<(x/y)<<endl;
	}
	catch(const exception& e){
		cout<<e.what()<<endl;
	}
	return 0;
}


//trasnspose matrix


#include <iostream>
using namespace std;

int main() {
    int matrix[10][10], transposed[10][10];
    int rows, cols;

    cout << "Enter number of rows and columns: ";
    cin >> rows >> cols;

    cout << "Enter matrix elements:\n";
    for (int i = 0; i < rows; i++)
        for (int j = 0; j < cols; j++)
            cin >> matrix[i][j];

    for (int i = 0; i < rows; i++)
        for (int j = 0; j < cols; j++)
            transposed[j][i] = matrix[i][j];

    cout << "Transposed Matrix:\n";
    for (int i = 0; i < cols; i++) {
        for (int j = 0; j < rows; j++)
            cout << transposed[i][j] << " ";
        cout << endl;
    }

    return 0;
}


//recursive function for 2 anagrams


#include <iostream>
#include <string>
using namespace std;

bool areAnagrams(string str1, string str2) {
    if (str1.length() != str2.length()) {
        return false;
    }
    
    int count[256] = {0};
    for (char c : str1) {
        count[c]++;
    }

    for (char c : str2) {
        count[c]--;
        if (count[c] < 0) {
            return false;
        }
    }

    return true; 
}

int main() {
    string str1, str2;

    cout << "Enter first string: ";
    cin >> str1;
    cout << "Enter second string: ";
    cin >> str2;

    if (areAnagrams(str1, str2)) {
        cout << str1 << " and " << str2 << " are anagrams." << endl;
    } else {
        cout << str1 << " and " << str2 << " are not anagrams." << endl;
    }

    return 0;
}


//subarrays


#include <iostream>
using namespace std;

int main() {
    int n;

    cout << "Enter the number of elements in the array: ";
    cin >> n;

    int arr[n];
    cout << "Enter the elements of the array:\n";
    for (int i = 0; i < n; i++) {
        cin >> arr[i];
    }

    cout << "Subarrays of the given array:\n";
    for (int start = 0; start < n; start++) {
        for (int end = start; end < n; end++) {
            for (int k = start; k <= end; k++) {
                cout << arr[k] << " ";
            }
            cout << endl; 
        }
    }

    return 0;
}



//online shopping system


#include <iostream>
#include <vector>
#include <string>

using namespace std;

class Item {
public:
    string name;
    float price;

    Item(string n, float p) : name(n), price(p) {}
};

class ShoppingCart {
public:
    vector<Item> cart;

    void addItem(const Item& item) {
        cart.push_back(item);
        cout << item.name << " added to cart." << endl;
    }

    void displayCart() {
        if (cart.empty()) {
            cout << "Your cart is empty." << endl;
            return;
        }

        cout << "Items in your cart:" << endl;
        for (const auto& item : cart) {
            cout << "- " << item.name << ": $" << item.price << endl;
        }
    }

    float checkout() {
        float total = 0;
        for (const auto& item : cart) {
            total += item.price;
        }
        cart.clear(); 
        return total;
    }
};

int main() {
    vector<Item> items = {
        Item("Laptop", 999.99),
        Item("Smartphone", 599.99),
        Item("Headphones", 199.99),
        Item("Smartwatch", 299.99)
    };

    ShoppingCart cart;
    int choice;

    do {
        // Display available items
        cout << "\nAvailable items:" << endl;
        for (size_t i = 0; i < items.size(); i++) {
            cout << i + 1 << ". " << items[i].name << ": $" << items[i].price << endl;
        }

        cout << "Enter item number to add to cart (0 to checkout, -1 to exit): ";
        cin >> choice;

        if (choice > 0 && choice <= items.size()) {
            cart.addItem(items[choice - 1]);
        } else if (choice == 0) {
            cart.displayCart();
            float total = cart.checkout();
            cout << "Total: $" << total << endl;
            cout << "Thank you for shopping!" << endl;
        } else if (choice == -1) {
            cout << "Exiting..." << endl;
        } else {
            cout << "Invalid choice. Try again." << endl;
        }

    } while (choice != -1);

    return 0;
}



//device with an attribute device name


#include <iostream>
#include <string>

using namespace std;

class Device {
public:
    string deviceName; 

    Device(string name) : deviceName(name) {}

    void display() {
        cout << "Device Name: " << deviceName << endl;
    }
};

int main() {
    Device myDevice("Smartphone");
    myDevice.display();

    return 0;
}


//uppercase using pointers



#include <iostream>

using namespace std;

void toUpperCase(char* str) {
    while (*str) { 
        if (*str >= 'a' && *str <= 'z') {
            *str -= 32; 
        }
        str++; 
    }
}

int main() {
    char str[] = "hello"; 

    toUpperCase(str); 

    cout << "Uppercase string: " << str << endl; 

    return 0;
}



//2D arrays in spiral order


#include <iostream>
using namespace std;

void printSpiral(int arr[3][3]) {
    int top = 0, bottom = 2, left = 0, right = 2; 

    while (top <= bottom && left <= right) {
        for (int i = left; i <= right; i++) {
            cout << arr[top][i] << " ";
        }
        top++;

        for (int i = top; i <= bottom; i++) {
            cout << arr[i][right] << " ";
        }
        right--;

        if (top <= bottom) {
            for (int i = right; i >= left; i--) {
                cout << arr[bottom][i] << " ";
            }
            bottom--;
        }

        if (left <= right) {
            for (int i = bottom; i >= top; i--) {
                cout << arr[i][left] << " ";
            }
            left++;
        }
    }
}

int main() {
    int arr[3][3] = { {1, 2, 3}, {4, 5, 6}, {7, 8, 9} }; 

    cout << "Spiral Order: ";
    printSpiral(arr); 
    cout << endl;

    return 0;
}



//rotate array(90)


#include <iostream>
using namespace std;

void rotate90Clockwise(int arr[3][3]) {
    int n = 3;

    for (int i = 0; i < n; i++) {
        for (int j = i; j < n; j++) {
            swap(arr[i][j], arr[j][i]);
        }
    }

    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n / 2; j++) {
            swap(arr[i][j], arr[i][n - j - 1]);
        }
    }
}

void printMatrix(int arr[3][3]) {
    for (int i = 0; i < 3; i++) {
        for (int j = 0; j < 3; j++) {
            cout << arr[i][j] << " ";
        }
        cout << endl;
    }
}

int main() {
    int arr[3][3] = {
        {1, 2, 3},
        {4, 5, 6},
        {7, 8, 9}
    };

    cout << "Original Matrix:" << endl;
    printMatrix(arr);

    rotate90Clockwise(arr);

    cout << "Rotated Matrix:" << endl;
    printMatrix(arr);

    return 0;
}




//Concatenate string


#include <iostream>
#include <string>
using namespace std;

int main() {
    string str1, str2, result;

    cout << "Enter first string: ";
    getline(cin, str1);
    cout << "Enter second string: ";
    getline(cin, str2);

    result = str1 + str2; 

    cout << "Concatenated String: " << result << endl;

    return 0;
}


//sum of non adjacent 

#include <iostream>
using namespace std;

int maxNonAdjacentSum(int arr[], int n) {
    int include = 0;
    int exclude = 0;

    for (int i = 0; i < n; i++) {
        int num = arr[i];
        int newExclude = max(include, exclude);
        include = exclude + num;
        exclude = newExclude;
    }

    return max(include, exclude);
}

int main() {
    int arr[] = {3, 2, 5, 10, 7};
    int n = sizeof(arr) / sizeof(arr[0]);

    int maxSum = maxNonAdjacentSum(arr, n);
    cout << "Maximum sum of non-adjacent elements: " << maxSum << endl;

    return 0;
}
